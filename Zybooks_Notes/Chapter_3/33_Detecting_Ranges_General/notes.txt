**Detecting Ranges Using if-elseif-else**
    - This can detect number ranges with each branch performing a different action
    for each range. Each expression only needs to indicate the upper range part. 